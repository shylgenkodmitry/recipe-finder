/*
 * HomePage Messages
 *
 * This contains all the text for the HomePage component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  searchResultNoData: {
    id: 'recipefinder.containers.HomePage.search_result.NoData',
    defaultMessage: 'No Data!',
  },
});
